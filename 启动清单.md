# 红山动物星球 — 启动检查清单

> 跟着做 3 步，就能跑起来。每步都有 ✅ 确认标志，看到它就说明成功了。

---

## 第 1 步：安装 Node.js

去 https://nodejs.org 下载 **LTS 版本**，双击安装，一路点"下一步"。

安装完后打开 PowerShell（按 `Win + R`，输入 `powershell`，回车），粘贴这行：

```powershell
node -v
```

✅ **看到类似 `v20.11.0` 的版本号 → 成功**
❌ 显示"不是内部或外部命令" → 重启电脑后再试

---

## 第 2 步：填入你的 API Key

1. 打开 https://bailian.console.aliyun.com ，注册并创建一个 API Key
2. 在"模型广场"里搜索并开通这两个模型：
   - `qwen-plus`
   - `paraformer-realtime-v2`
3. 用记事本打开项目里的 `js/llm.js` 文件
4. 找到第 10 行，把 `sk-your-dashscope-api-key-here` 替换成你的 Key，保存

替换后长这样（别丢了引号）：

```
apiKey: 'sk-你自己的Key',
```

✅ **保存文件后，Key 里没有 `your` `here` 这些字 → 成功**
❌ 还是占位符 → 语音功能无法使用

---

## 第 3 步：启动两个服务

打开**两个** PowerShell 窗口，都先 `cd` 进项目目录：

```powershell
cd 项目解压后的路径
```

**窗口 1** — 粘贴这行（启动网页）：
```powershell
npx serve .
```
> 如果提示 `Ok to proceed? (y)`，输入 `y` 回车

✅ **看到 `http://localhost:3000` → 成功**

**窗口 2** — 粘贴这行（启动语音）：
```powershell
node voice-proxy.js
```

✅ **看到 `语音识别代理已启动` → 成功**

---

## 全部启动后

浏览器打开 **http://localhost:3000** ，点击"开始探索"即可。

进入场馆后点麦克风按钮说话，能识别成文字就全部跑通了。🎧

---

## 卡住了？

详细排查见 **使用指南.md** 的"错误排查"章节。
