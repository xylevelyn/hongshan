# 红山动物星球 Demo — 使用指南

## 一、环境准备（3 件事）

### 第 1 件：安装 Node.js

**为什么需要**：项目用 `npx serve` 启动静态服务器，用 `node voice-proxy.js` 启动语音代理，都需要 Node.js。

**操作步骤**：

1. 访问 https://nodejs.org/zh-cn
2. 下载 **LTS 版本**（v18 或更高）
3. 双击安装包，一路"下一步"即可（默认会配置 PATH 环境变量）
4. 安装完成后，打开 **PowerShell**（Win+R 输入 `powershell`），验证安装：

```powershell
node -v
# 应输出类似：v20.11.0

npm -v
# 应输出类似：10.2.4
```

> **Mac 用户**：推荐用 Homebrew 安装：`brew install node`

---

### 第 2 件：配置 API Key

**为什么需要**：项目的 AI 对话和语音识别功能依赖阿里云百炼（DashScope）的大模型服务，需要你自己的 API Key。

**操作步骤**：

1. **注册阿里云账号**并登录：https://bailian.console.aliyun.com
2. **开通百炼服务**：首次进入控制台会提示开通，按指引完成
3. **创建 API Key**：控制台左侧菜单 → "API-KEY 管理" → "创建新的 API-KEY"
4. **开通模型授权**：
   - 进入"模型广场"，搜索并开通以下两个模型：
     - `qwen-plus`（文本对话）
     - `paraformer-realtime-v2`（语音识别）
   - 新用户通常有免费额度
5. **填入项目**：用记事本或 VS Code 打开项目中的 `js/llm.js`，找到第 10 行：

```javascript
apiKey: 'sk-your-dashscope-api-key-here',  // ← 替换为你的 Key
```

将 `sk-your-dashscope-api-key-here` 替换为你刚才创建的 Key（保留引号），保存文件。

---

### 第 3 件：启动服务并打开浏览器

项目需要同时运行**两个服务**，请打开**两个 PowerShell 窗口**：

**窗口 1 — 启动网页服务器**

```powershell
cd 项目解压目录
# 例如：cd C:\Users\你的用户名\Downloads\zoo-demo
npx serve .
```

首次运行会提示安装 `serve` 包，输入 `y` 回车即可。启动成功后会显示：

```
INFO: Accepting connections at http://localhost:3000
```

**窗口 2 — 启动语音识别代理**（仅语音输入功能需要）

```powershell
cd 项目解压目录
node voice-proxy.js
```

启动成功后会显示：

```
[Proxy] API Key: sk-your-key...
[Proxy] 语音识别代理已启动: ws://localhost:53857
```

**打开浏览器**：

访问 `http://localhost:3000`，点击"开始探索"即可体验。

> **语音输入功能**：进入场馆对话页后，点击麦克风按钮，浏览器会弹出麦克风权限请求，选择"允许"。然后按住"按住说话"按钮说话即可。

---

## 二、错误排查示例

### 错误 1：`npx serve` 提示命令未找到

**现象**：
```
'npx' 不是内部或外部命令
```

**原因**：Node.js 未安装或未正确配置 PATH。

**解决**：
1. 重新安装 Node.js（参考第 1 件）
2. 安装后**关闭并重新打开** PowerShell 窗口
3. 再次运行 `node -v` 验证

---

### 错误 2：API Key 报错 403 AccessDenied

**现象**：对话时提示"网络似乎有些不稳定"，浏览器控制台（F12）显示：
```
API错误: 403 AccessDenied.Unpurchased
```

**原因**：API Key 未开通对应模型的授权。

**解决**：
1. 登录 https://bailian.console.aliyun.com
2. 进入"模型广场"
3. 搜索 `qwen-plus`，点击"开通"
4. 搜索 `paraformer-realtime-v2`，点击"开通"
5. 等待几分钟生效后重试

---

### 错误 3：端口 3000 被占用

**现象**：
```
ERROR: Port 3000 is already in use
```

**原因**：其他程序占用了 3000 端口。

**解决**：换一个端口启动：
```powershell
npx serve . -l 8080
```
然后浏览器访问 `http://localhost:8080`

---

### 错误 4：语音代理启动失败

**现象 1**：
```
[Proxy] 未找到 API Key
```

**原因**：`js/llm.js` 中的 apiKey 还是占位符。

**解决**：按第 2 件填写真实 API Key。

**现象 2**：
```
Error: listen EADDRINUSE: address already in use :::53857
```

**原因**：上次的代理进程未关闭。

**解决**：关闭旧的代理窗口，或运行：
```powershell
# 查找占用 53857 端口的进程
netstat -ano | findstr 53857
# 用进程 ID（最后那列数字）结束进程
taskkill /PID 进程ID /F
```

---

### 错误 5：语音输入按钮无反应

**现象**：点击麦克风按钮后没反应，或提示"无法访问麦克风"。

**排查步骤**：

1. **确认代理已启动**：检查窗口 2 是否显示"语音识别代理已启动"
2. **确认协议**：浏览器地址栏必须是 `http://localhost` 或 `http://127.0.0.1`，**不能是直接双击 HTML 文件打开**（file:// 协议下麦克风无法工作）
3. **检查麦克风权限**：
   - Chrome：地址栏左侧的锁/感叹号图标 → 麦克风 → 允许
   - Edge：地址栏左侧图标 → 网站权限 → 麦克风 → 允许
4. **检查系统麦克风**：Windows 设置 → 隐私 → 麦克风 → 确保开启

---

### 错误 6：浏览器控制台 CORS 错误

**现象**：F12 控制台显示：
```
Access to fetch at 'https://dashscope.aliyuncs.com/...' from origin 'http://localhost:3000'
has been blocked by CORS policy
```

**原因**：可能是网络代理或 VPN 干扰了跨域请求。

**解决**：
1. 关闭 VPN 或系统代理后重试
2. 如果必须用代理，确保代理放行了 `dashscope.aliyuncs.com` 域名

---

### 错误 7：AI 回复一直转圈不出字

**现象**：发送问题后"红山正在思考..."一直不消失。

**排查步骤**：

1. 按 F12 打开开发者工具 → Network 面板
2. 重新发送问题，查看是否有 `chat/completions` 请求
3. 如果请求状态是 `pending`：网络慢，等待即可
4. 如果请求状态是 `403`：参考错误 2
5. 如果请求状态是 `429`：API 调用频率超限，等 1 分钟再试
6. 如果没有请求：检查网络连接是否正常

---

### 错误 8：语音朗读没声音

**现象**：点击"🔊 朗读"按钮后没声音。

**排查步骤**：

1. 检查电脑音量和浏览器静音状态
2. 检查浏览器是否支持 Web Speech API：在地址栏输入
   ```
   javascript:alert('speechSynthesis' in window)
   ```
   应弹出 `true`。推荐使用 Chrome 或 Edge 浏览器
3. 首次加载时声音库需要初始化，刷新页面后重试

---

## 三、常见问题

**Q：不用语音输入功能，只开一个服务可以吗？**
A：可以。只运行 `npx serve .` 就能使用对话、朗读、拍照、故事生成等功能，语音输入会自动回退到浏览器自带的 Web Speech API（Chrome/Edge 支持）。

**Q：可以部署到服务器让多人访问吗？**
A：不建议直接部署。API Key 在前端是公开的，任何人都能看到。生产环境需要加一层后端代理来保管 Key。

**Q：手机能用吗？**
A：可以访问页面，但语音输入功能需要 `localhost` 或 HTTPS 环境。如需手机使用，建议部署到 HTTPS 域名并加后端代理。

**Q：数据会上传到哪里？**
A：对话内容会发送到阿里云百炼（DashScope）处理。本地不保存任何用户数据，关闭浏览器即清除。

---

## 四、技术支持

如果以上排查仍无法解决，请提供以下信息寻求帮助：

1. 操作系统（Windows / Mac / Linux）
2. 浏览器类型和版本（Chrome 120 / Edge 120 等）
3. **浏览器控制台错误信息**（F12 → Console 面板，截图或复制红色错误）
4. 代理窗口的错误信息（如果有）
5. 复现步骤（做了什么操作 → 出现什么现象）
